package com.example.cdc.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class PrometheusConfig {

    /*
     Spring Boot exposes Prometheus metrics through:

     /actuator/prometheus

     Enable it in application.properties
    */

}